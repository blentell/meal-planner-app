import React from "react";

export const SearchContext = React.createContext({});
export const RecipeContext = React.createContext({});
export const NewRecipeContext = React.createContext({});


